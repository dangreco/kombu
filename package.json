{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "lerna run clean",
    "build": "lerna run build",
    "check": "lerna run check",
    "lint": "lerna run lint",
    "format": "lerna run format",
    "release": "npm run clean && npm run lint && npm run format && npm run build && npm run check && lerna publish"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.2",
    "lerna": "^7.1.3",
    "shx": "^0.3.4"
  }
}
